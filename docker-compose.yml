version: "3.8"

services:
  mongo:
    image: mongo:7
    container_name: structify-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    # No auth for local development; server connects to mongodb://localhost:27017/structify

volumes:
  mongo_data:
